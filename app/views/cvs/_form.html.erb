<%= simple_form_for @cv , html: {:class => 'form'} do |form| %>
  <% if @cv.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(formabc.errors.count, "error") %> prohibited this cv from being saved:</h2>

      <ul>
        <% @cv.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block", :class => 'input__label'%>
    <%= form.text_field :name , :class => 'input__field--text' %>
  </div>

  <div>
    <%= form.label :surname, style: "display: block", :class => 'input__label'  %>
    <%= form.text_field :surname, :class => 'input__field--text' %>
  </div>

  <div>
    <%= form.label :patrnymic, style: "display: block", :class => 'input__label' %>
    <%= form.text_field :patronymic, :class => 'input__field--text' %>
  </div>

  <div class="form form__group form__group--inline">
    <div>
      <%= form.submit 'Save', :class => 'btn btn--primary'%>
    </div>

    <div>
      <%= link_to "Cancel", :back, :class => 'btn btn--danger' %>
    </div>
  </div>

<% end %>
